//not added in app layout yet. idk if footer is necessary. will delete later if not needed

const Footer = () => {
  return (
    <footer className="p-4 bg-white md:p-8 lg:p-10">
      <div className="mx-auto max-w-screen-xl text-center">
        <p>Footer</p>
      </div>
    </footer>
  );
};

export default Footer;
