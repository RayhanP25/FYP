# Copy to .env and adjust if your webcams are not at indices 0 and 1.
# Run: curl http://localhost:8000/api/camera/list  (after server starts) to see detected indices.
CAMERA_LEFT_INDEX=0
CAMERA_RIGHT_INDEX=1
CAMERA_OPEN_TIMEOUT_SEC=10
